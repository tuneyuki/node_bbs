<!DOCTYPE html>
<html lang="ja">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <title>{{{title}}}</title>
  </head>

  <body>
    <div class="header">
      <header>
        <a href="/bbs"><h1 class="title">{{{title}}}</h1></a>
      </header>
    </div><!-- /header -->
    <div id="contents">

      <div class="inputArea">
        {{#if update}}
          {{#update}}
          <form action="/bbs/update/{{_id}}" method="post">
            <textarea name="text" rows="10" wrap="hard">{{post}}</textarea>
            <button type="submit">Submit</button>
          </form>
          {{/update}}
        {{else}}
          <form action="/bbs/post" method="post">
            <textarea name="text" rows="10" wrap="hard"></textarea>
            <button type="submit">Submit</button>
          </form>
        {{/if}}
      </div><!-- /inputArea -->


      <div class="listArea">
        <form action="/bbs/edit" method="POST">
        {{#posts}}
          <pre>{{post}}</pre><button type="submit" name="_id" value="{{_id}}">編集</button>
        {{/posts}}
        </form>
      </div><!-- /listArea -->

    </div><!-- /contents -->
    <div class="footer">
      <footer class="mainFooter">
        <p>copyright &copy; Hoge</p>
      </footer>
    </div>
  </body>

</html>